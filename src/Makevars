SOURCES= wrapper.cpp $(wildcard fdasrsf/*.cpp gropt/*/*.cpp)

OBJECTS = $(SOURCES:.cpp=.o)

PKG_CPPFLAGS=-I. -Igropt/incl/
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS)

CXX_STD = CXX11

all: $(SHLIB)

#$(OBJECTS): clean

clean:
	@rm -f $(OBJECTS)
